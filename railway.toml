[build]
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "timeout 30 alembic upgrade head; uvicorn app.main:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
