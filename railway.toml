[build]
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "python -c \"import app.main; print('import OK')\" && uvicorn app.main:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
